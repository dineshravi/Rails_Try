<%= form_for(@contents, :url => "/submit" ) do |f| %>

	<div class = "field">
		<%=f.label :commenter %></br>
		<%=f.text_field :commenter %>
	</div>

	<div class = "field">
		<%= f.label :body %></br>
		<%= f.text_area :body %>
	</div>

	<div  class ="action">
		<%= f.submit 'Submit', :id=> 'sub1' %>
	</div>

	<% end%>
		<div class="error">

	</div>	
