---
applications:
  .:
    name: rails_try
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    url: ${name}.${target-base}
    mem: 256M
    instances: 1
    services:
      mysql-2381b:
        type: mysql
